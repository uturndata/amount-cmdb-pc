CREATE TABLE resource_config (
    id bigserial
    resources_id
    resource_id varchar
    version int
    version_date datetime
    json jsonb
) PRIMARY KEY (id)

CREATE TABLE resource_terraform (
    id bigserial
    resources_id
    resource_id varchar
    version int
    version_date datetime
    json jsonb
) PRIMARY KEY (id)