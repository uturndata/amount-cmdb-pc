CREATE TABLE resource_config (
    id bigserial
    resource_id varchar
    version_date datetime
    json jsonb
) PRIMARY KEY (id)

CREATE TABLE resource_terraform (
    id bigserial
    resource_id varchar
    version_date datetime
    json jsonb
) PRIMARY KEY (id)